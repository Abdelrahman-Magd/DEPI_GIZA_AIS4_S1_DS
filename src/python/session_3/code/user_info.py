def info_user(name,age):
    """  
    param_1 : take y= the name from user
    type param_1 : str

    param_2 : take age from user
    type param_2 : int

    return : return a string of the name and the age
    type return : str

    """
    return f"my name is {name} and my age is {age} years old"